import React , {useState,useEffect,useContext} from "react";
import { Toggle } from "../index";

export const Token = ({openSetting}) => {  
    return (
        <div>
            
        </div>
    )
}